module Char

import Bool
import Nat

type Class = either {
  .any!,
  .char Char,
  .whitespace!,
  .ascii either {
    .any!,
    .alpha!,
    .alphanum!,
    .digit!,
  },
}

def Equals : [Char, Char] Bool = external
def Code   : [Char] Nat = external
def Is     : [Char, Class] Bool = external
