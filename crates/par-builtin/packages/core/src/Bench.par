module Bench

// Identity function that prevents the compiler from optimizing away its argument.
// Useful in benchmarks to ensure a computation actually runs.
dec BlackBox : [type a] [a] a
def BlackBox = external
