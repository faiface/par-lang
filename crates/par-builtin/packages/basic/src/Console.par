module Console

import @core/Option
import @core/String

type Console = iterative choice {
  .close => !,
  .print(String) => self,
  .prompt(String) => (Option<String>) self,
}

def Open : Console = external
