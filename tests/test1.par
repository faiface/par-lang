// Simple test file

def Add: [Nat, Nat] Nat = [a, b]
  Nat.Add(a, b)

def TestSimple: [Test] ! = [test] do {
  test.assert("2 + 2 = 4", Nat.Equals(Add(2, 2), 4))
  test.assert("10 + 5 = 15", Nat.Equals(Add(10, 5), 15))
} in !
